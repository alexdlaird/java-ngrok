name: "CI/CD"

on:
  workflow_dispatch:
  push:
    branches: [ main, develop, java8-ngrok ]
  pull_request:
    branches: [ main, develop, java8-ngrok ]
  schedule:
    - cron: "0 10 * * *"

jobs:
  build:
    name: Build

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 8
        uses: actions/setup-java@v2
        with:
          java-version: 8
          distribution: adopt
      - name: Install dependencies
        run: make install
      - name: Run tests
        run: make test
      - name: Upload coverage
        uses: codecov/codecov-action@v1
        with:
          file: ./build/reports/jacoco/report.xml
